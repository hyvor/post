## Development

```sh
# reset database and migrate (drops and recreates the databse)
bin/console app:dev:reset

# reset and seed
bin/console app:dev:reset --seed

# seed only
bin/console app:dev:seed
```
